/* function getUSer(accessToken:string){
    
}

// autenticar usuario
async function checkAuth() {
  if(accessToken){

  }else{
    const token = getRefresToken();
    if(token){

    }
  }
}
 */

export interface siu {
    body: {
      accessToken: string;
    };
  }